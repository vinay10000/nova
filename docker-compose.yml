services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: nova
      POSTGRES_PASSWORD: nova
      POSTGRES_DB: nova
    ports:
      - "5432:5432"
    volumes:
      - nova-pg:/var/lib/postgresql/data

volumes:
  nova-pg:
